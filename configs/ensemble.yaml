ensemble:
  n_members: 3
  base_seed: 100
model:
  hidden: [64, 64, 64]
training:
  epochs: 200
  batch_size: 512
  lr: 0.002
data:
  train: data/transport_train.npz
  validation: data/transport_validation.npz
output_dir: outputs/ensemble
